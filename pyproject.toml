[project]
name = "crypto-ai-trading-bot"
version = "0.8.0"
description = "Safety-first Binance Spot Testnet engine with AI review, research lab and parallel Spot Testnet and isolated Futures Demo forward testing"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
portal = ["waitress==3.0.2"]
updates = ["cryptography==50.0.1"]

[project.scripts]
crypto-trader = "trader.cli:main"

[tool.unittest]
start-directory = "tests"
