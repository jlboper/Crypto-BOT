[project]
name = "crypto-ai-trading-bot"
version = "0.8.2"
description = "Safety-first dual-engine Spot Testnet and Futures Demo forward testing with independent risk controls and modern Windows operations UI"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
portal = ["waitress==3.0.2"]
updates = ["cryptography==50.0.1"]

[project.scripts]
crypto-trader = "trader.cli:main"

[tool.unittest]
start-directory = "tests"
