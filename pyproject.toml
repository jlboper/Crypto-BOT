[project]
name = "crypto-ai-trading-bot"
version = "0.6.21"
description = "Safety-first multi-asset Binance Spot bot with constrained AI review and research lab"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
portal = ["waitress==3.0.2"]
updates = ["cryptography==50.0.1"]

[project.scripts]
crypto-trader = "trader.cli:main"

[tool.unittest]
start-directory = "tests"
