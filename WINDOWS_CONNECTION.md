# Conexión Windows — 15-09-2026

El portal https://crypto-paper-private-portal.jlboper.workers.dev recibe telemetría PAPER real del agente iniciado desde esta copia aislada. La consulta autenticada confirmó connected=true y una instantánea recibida; evidencia sin valores financieros ni claves en cloudflare/connection-verification.json. No se enviaron comandos de prueba contra la instalación operativa.

El agente lee ../data/trader.db con SQLite mode=ro. Conserva su bloqueo, checkpoint, estado de conexión y archivos de salida en data/ dentro de esta copia. No inicia el motor, no carga sus claves OpenAI/Binance y no hace llamadas al exchange ni a IA. Comprueba PAPER y rutas antes de sincronizar. Sondeo aproximado de 30 segundos; reintentos con espera creciente hasta 5 minutos. Memoria observada alrededor de 30 MB; no es una medición sostenida.

Los controles autorizados del portal actúan sobre ../data/KILL_SWITCH: Pausar crea el interruptor solo si no existe; Reanudar solo elimina una pausa creada por el agente. Una pausa local/automática existente se conserva y su liberación requiere atención local. Una solicitud rechazada puede permanecer pendiente hasta vencer. El indicador «Aplicada por Windows» confirma el cambio del archivo, no una respuesta inmediata del motor. La instalación original observa ese interruptor durante su ciclo; no hay garantía de pausa instantánea. El motor original aún no incluye las mejoras de la copia auditada.

El agente sigue activo en segundo plano. Inicio automático configurado mediante la tarea `Crypto Paper Portal Agent`: se ejecuta 30 segundos después de iniciar la sesión del propietario, con pythonw sin ventana, permisos limitados y sin guardar contraseña. No se ejecuta antes de iniciar sesión. Permite batería, no tiene límite de duración, ignora arranques duplicados y reintenta hasta tres veces con intervalo de un minuto si el proceso falla. La desconexión de Internet se gestiona dentro del agente con espera creciente.

Se probó el arranque desde el Programador de tareas: tarea Running, PID nuevo del agente, sincronización PAPER confirmada por el portal y motor original con el mismo PID. No se reinició Windows para simular el siguiente inicio de sesión. La definición exportada está en data/remote-autostart-task.xml y el instalador reproducible en scripts/install_agent_autostart.ps1. Mantener esta copia y el runtime Python en sus rutas actuales; si se mueven habrá que actualizar la tarea.

Para detener solo la conexión, ejecutar scripts/stop_portal_agent.ps1; solicita salida mediante data/REMOTE_STOP, sin parar el motor ni cambiar su pausa. La salida puede tardar hasta el timeout HTTPS de 15 segundos más el intervalo de comprobación. La siguiente ejecución automática elimina esa solicitud de parada temporal. Para desactivar el inicio automático de forma persistente, deshabilitar `Crypto Paper Portal Agent` en el Programador de tareas y ejecutar el script de parada. Para arrancarlo manualmente sin usar la tarea, scripts/start_portal_agent.ps1 utiliza el Python validado y se niega si sigue vivo el PID registrado. El archivo data/remote-status.json muestra la última sincronización y, si falla, únicamente el tipo de error.

La clave de acceso del propietario sigue siendo la que ya guardaste. El token del dispositivo permanece en cloudflare/.secrets/windows-agent.env con ACL restringida. No copiarlo al chat ni incluir esa carpeta en repositorios o respaldos compartidos.

Validación de conexión: 59 pruebas Python offline aprobadas, cero fallos. Pruebas añadidas: aislamiento de checkpoint y lectura de BD, pausa local preservada, rechazo LIVE, rechazo de base inexistente, guardia antes de red y parada del bucle. La configuración de autoinicio se verificó después mediante su ejecución real y una consulta autenticada al portal. Pendientes: comprobación tras reiniciar Windows, control integral sobre un motor de prueba, comprobación en teléfono físico y migración controlada del motor auditado. No se reinició ni sustituyó la instancia original.
