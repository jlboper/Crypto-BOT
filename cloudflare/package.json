{
  "name": "crypto-paper-private-portal",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "dev": "wrangler dev --local",
    "check": "wrangler deploy --dry-run",
    "deploy": "node scripts/deploy.mjs"
  },
  "devDependencies": {
    "wrangler": "4.131.1"
  }
}
